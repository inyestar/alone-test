spring.application.name=config
# it disable auto-decryption on encrypted property 
spring.cloud.config.server.encrypt.enabled=false
# if you want to use bootstrap properties file, spring-cloud-stater-bootstrap must be added.
# you can check here : TextEncryptorConfigBootstrapper [entry] - keysConfigured(KeyProperties)
encrypt.keyStore.location=classpath:config-server-user.jks
encrypt.keyStore.password=freeus
encrypt.keyStore.alias=alone-config-user