# 자동으로 암호화된 프로퍼티를 복호화하여 클라이언트에 전달하는 것을 방지
spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false
encrypt:
  keyStore:
    location: classpath:config-server-user.jks
    password: freeus
    alias: alone-config-user